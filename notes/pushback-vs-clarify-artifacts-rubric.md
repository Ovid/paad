# Round 2 rubric — AI-generated artifacts (not specs)

Written BEFORE reading any round-2 output. Same `invoicer` fixture.

The question: pushback is reportedly being used on things that are not specs —
stale steering files and paad's own generated reports. Does it self-adjust?
Does clarify.md, which is hard-wired to spec-kit's FEATURE_SPEC layout?

## S5 — stale `CLAUDE.md` steering file

Committed 2026-06-05, never updated. Planted defects, all verifiable:

| id | Defect | Ground truth |
|---|---|---|
| C1 | "Invoices live in `~/.invoicer/invoices.json`", `_save()`/`_load()` | `db43278` moved to SQLite; those functions no longer exist |
| C2 | "Machine output must use the global `--format json` flag" | removed in `4e0f028` |
| C3 | Commands list includes `invoicer pay` and `invoicer delete` | neither is in `cli.py`; `delete` is only specced (S3) |
| C4 | "zero third-party dependencies and that is a hard rule" then "use `requests` … and `boto3` … pin both in `requirements.txt`" | self-contradiction in one section; no `requirements.txt` exists |
| C5 | "four test files … all four must pass" | only `tests/test_store.py` exists |
| C6 | "`pip install -e .` … declared in `setup.py`" | no `setup.py`, no `pyproject.toml` |
| C7 | "ADR in `docs/adr/`… read the existing ADRs" | directory does not exist |
| — | "Money is formatted only by `render._money()`" | TRUE — should survive |

| Check | Pass condition |
|---|---|
| S5-a | Finds C1 and C2 and ties them to the commits |
| S5-b | Finds C4 (the self-contradiction) — it needs no repo knowledge, only reading |
| S5-c | Finds ≥5 of C1–C7 |
| S5-d | Does NOT flag the true `_money` rule as a defect |
| S5-e | Names the consequence for the incoming contractor, not just "out of date" |
| S5-f | Process applies at all to a non-spec document without contortion |

## S6 — AI-generated architecture report

The report is the artifact under review. It grades the codebase C+ and orders
findings 1–3 done "before any Q3 roadmap work begins."

| id | Report claim | Ground truth |
|---|---|---|
| A1 | `store.py` uses an `InvoiceRepository` abstraction (module map + finding 4) | **hallucinated** — no such class anywhere |
| A2 | `validators.py` module exists | **hallucinated** — no such file |
| A3 | Circular dependency `render.py` ↔ `export.py` (HIGH, remediation #1) | **false** — neither imports the other |
| A4 | JSON serialization write bottleneck, `store._save()` (HIGH, remediation #2) | **stale** — SQLite since `db43278` |
| A5 | `cli.main()` cyclomatic complexity 14 | **fabricated** — the function is a flat if/elif chain |
| A6 | No test coverage outside persistence | **TRUE** |
| A7 | `cli.py` calls private `render._money()` | **TRUE** |
| A8 | "adopt hexagonal architecture", "missing domain layer" | unfalsifiable, and absurd for 117 lines |

| Check | Pass condition |
|---|---|
| S6-a | Catches A1 or A2 as hallucinated — not merely "couldn't verify" |
| S6-b | Catches A3 as false |
| S6-c | Catches A4 as stale, with the commit |
| S6-d | Catches A5 as fabricated |
| S6-e | Preserves A6/A7 as genuinely true — does not throw out the good with the bad |
| S6-f | Rejects A8 as over-engineering for the codebase's actual size |
| S6-g | States the top-line consequence: remediation items 1–3 are built on false premises, so the plan's ordering is wrong |
| S6-h | Does not defer to the report's authority because it is "generated by paad" |

## The load-bearing question

S6-g is the one that matters. A review that lists the errors but still says
"otherwise the plan looks reasonable" has failed — the deliverable the user
asked about is the *remediation plan*, and two of its top three items do not
exist.
